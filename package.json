{
  "name": "cab-booking-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
